#function that takes a string as a parameter,coverts it to a float and returns the result

def convert(a):
    """
    string has to be a number "100"
    
    """
    result=float(a)
    return result

converter=convert("888")
print(type(converter))
